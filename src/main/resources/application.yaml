spring:
    application:
        name: CommerceX

    profiles:
        active: dev

    docker:
        compose:
            enabled: false

server:
    port: 8080

management:
    endpoints:
        web:
            exposure:
                include: health,info,metrics

logging:
    level:
        root: INFO
